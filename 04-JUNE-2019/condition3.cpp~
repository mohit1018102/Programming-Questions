#include<iostream>
#include<cmath>
using namespace std;
int main()
{

 int na;
 cin>>na;
 int a[na];
 
 int i,j;
 
 for(i=0;i<na;i++)
 {
     cin>>a[i];
 }
 
 int nb;
 cin>>nb;
 
 int b[nb];
 for(i=0;i<nb;i++)
 {
     cin>>b[i];
 }
 
 
 
 
 for(i=0;i<na;i++)
 {
    for(j=0;j<nb;j++)
    {
       if(pow(a[i],b[j])>pow(b[j],a[i]))
       {
          cout<<a[i]<<b[j]<<" ";
       }
    }
 }


 return 0;
}
